<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <!-- Brand/logo -->
    <a class="navbar-brand" href="#">
        <img src="{{ asset('image/brand.jpeg') }}" alt="logo" style="width:40px;">
    </a>

    <!-- Links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" href="#">Link 1</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Link 2</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Link 3</a>
        </li>
    </ul>
    <div class="ml-auto">

        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            <span class="text-light h6 mr-3">{{ app.user.fullname }}</span>
            <a class="btn btn-sm btn-outline-secondary"
               href="{{ path('security_logout') }}">
                <i class="fa fa-sign-out pr-2"></i>Déconnection
            </a>
        {% else %}
            <a class="btn btn-sm btn-outline-info"
               href="{{ path('security_login') }}">
                <i class="fa fa-sign-in pr-2"></i>Connection
            </a>
        {% endif %}
    </div>
</nav>