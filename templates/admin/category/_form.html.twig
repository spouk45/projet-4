{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="text-right">
    <button class="btn btn-primary"><i class="far fa-edit mr-1"></i>{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}