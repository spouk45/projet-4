{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Inscription</h1>

        {{ include('user/_form.html.twig') }}

        <a href="{#{{ path('user_index') }}#}">back to list</a>
    </div>
{% endblock %}
